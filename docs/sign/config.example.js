// GitHub Cloud Signing — public client portal configuration
// Copy this file to config.js in the same folder on your GitHub repo.
// No secrets belong here — the PAT stays in SiteScop desktop settings only.
window.SITESCOP_SIGN_CONFIG = {
  owner: 'your-github-username',
  repo: 'your-repo-name',
  branch: 'main',
};
